import { Fragment } from "react";
import classes from "./Options.module.css";

const Options = () => {
  return (
    <Fragment>
      <div className={classes.gm}></div>
    </Fragment>
  );
};
export default Options;
